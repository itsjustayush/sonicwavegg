{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "pnpm build && rm -rf public && mkdir -p public && cp -R dist/public/. public/",
  "outputDirectory": "public"
}
